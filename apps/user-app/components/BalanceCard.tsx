import { LockKeyhole, WalletCards } from "lucide-react";

export const BalanceCard = ({ amount, locked }: { amount: number; locked: number }) => <section className="rounded-2xl border border-[#e1e5dc] bg-white p-5 sm:p-6"><div className="flex items-center justify-between"><div className="flex items-center gap-2"><span className="grid h-8 w-8 place-items-center rounded-full bg-[#e7f5dc] text-[#4a8630]"><WalletCards className="h-4 w-4" /></span><h2 className="text-base font-black">Wallet balance</h2></div><LockKeyhole className="h-4 w-4 text-[#7b8577]" /></div><div className="mt-6 grid grid-cols-3 gap-3"><div><p className="text-[11px] text-[#7b8577]">Available</p><p className="mt-1 text-lg font-black">₹{(amount / 100).toLocaleString("en-IN", { minimumFractionDigits: 2 })}</p></div><div><p className="text-[11px] text-[#7b8577]">Locked</p><p className="mt-1 text-lg font-black">₹{(locked / 100).toLocaleString("en-IN", { minimumFractionDigits: 2 })}</p></div><div><p className="text-[11px] text-[#7b8577]">Total</p><p className="mt-1 text-lg font-black text-[#4a8630]">₹{((amount + locked) / 100).toLocaleString("en-IN", { minimumFractionDigits: 2 })}</p></div></div></section>;
